import {Component} from '@angular/core';

/**
 * Bootstrapping the <body> tag makes it easier to style Bootstrap themes.
 */
@Component({
    selector: 'body',
    styleUrls: ['./Body.scss'],
    templateUrl: './Body.html'
})
export class BodyComponent {
}
