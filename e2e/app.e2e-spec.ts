import {AppPage} from './app.po';

describe('tooltips App', () => {
    let page: AppPage;

    beforeEach(() => {
        page = new AppPage();
    });

    it('should display welcome message', () => {
        // @todo Tests will be added at a latest date.
        // page.navigateTo();
        // expect(page.getParagraphText()).toEqual('Welcome to app!');
    });
});
